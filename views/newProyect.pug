extends layout

include mixins/createProyect

block content 
    .contenido-principal
        h1=pageName

        pre=vardump(proyect)

        if errors 
            each error in errors
                .alerta.error=error.text
        +createProyect(proyect)