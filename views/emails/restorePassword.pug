doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Restore Password
        p Hello, you have requested to reset your password, click on the following link. This link is temporary, if it expires, request another
        a(href=`${resetUrl}`) Go to restore the password
        p If you cannot access this link, visit #{resetUrl}
        p If you did not request this email, you can ignore it